
{% block body %}
    <section class="section_articles_slider">
        <div class="cover-container">
            <div class="container pt-5 pb-5">
                <div class="d-flex mx-5 pb-4">
                    <h2 class="me-auto">Вам это будет интересно</h2>
                </div>
                <div class="mt-4 mb-5">
                    <div class="owl-carousel section_articles_slider__list" style="margin: auto;">
                        {% if news %}
                            {% for item in news %}
                                <a href="{{ path('app_news_one', {code: item.code}) }}">
                                <div class="section_articles_slider__list_item card shadow-sm bg-transparent">
                                    <div class="card-img">
                                        <img class="img-fluid" loading="lazy" src="{{ item.photo }}" alt="{{ item.title }}">
                                    </div>
                                    <div class="card-body text-start">
                                        <small class="section_articles_slider__list_item__header text-muted">{{ item.date }}</small>
                                        <p class="section_articles_slider__list_item__text card-text mt-3">{{ item.title }}</p>
                                    </div>
                                </div>
                                </a>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}